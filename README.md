## video-2-ascii
A lightweight C++ application that captures video frames and converts them into ASCII art, displaying the result directly in the terminal. Utilizes OpenCV for video processing and maps pixel intensities to characters for a real-time visual effect.
